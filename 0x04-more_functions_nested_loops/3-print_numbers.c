#include "main.h"
/**
 * print_numbers - prints the numbers from 0 to 9
 *
 * Return: numbers
 */
void print_numbers(void)
{
	int nbr;

	for (nbr = 48 ; nbr <= 57 ; nbr++)
		_putchar(num);
	_putchar('\n');
}
